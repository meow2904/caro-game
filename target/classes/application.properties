spring.application.name=caro-game

# WebSocket allowed origins (comma-separated). Can also be provided via environment variable `ALLOWED_ORIGINS`.
# Examples:
# app.allowed-origins=http://localhost:3000
# app.allowed-origins=https://example.com,https://another.com
# If not set, defaults to `*` (allow all).

#app.allowed-origins=http://localhost:8080, https://acd6579db7f0.ngrok-free.app
